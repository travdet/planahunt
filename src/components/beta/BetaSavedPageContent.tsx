'use client';

export default function BetaSavedPageContent() {
  return (
    <div className="mx-auto max-w-3xl space-y-4 px-4 py-12">
      <h1 className="text-3xl font-semibold text-[var(--forest-dark)]">Saved WMAs</h1>
      <p className="text-sm text-[var(--stone-600)]">
        Favorites and reminders will sync across the classic and beta experiences soon. For now, manage saved areas from the
        classic view.
      </p>
    </div>
  );
}
